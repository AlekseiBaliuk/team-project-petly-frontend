import { Container } from "components/Container/Container.styled"
import { Logo } from "components/Logo/Logo"

export const Header = () => {
    return(
        <header>
            <Container>
                <Logo />
            </Container>
        </header>
    )
}